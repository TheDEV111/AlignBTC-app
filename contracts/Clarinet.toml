[project]
name = 'alignbtc-underwriter'
description = 'AlignBTC — Autonomous Credit-Risk Underwriter escrow stub (Stacks grant)'
authors = ['Henry Agukwe']
telemetry = false
cache_dir = './.cache'
requirements = []
[contracts.credit-underwriter]
path = 'contracts/credit-underwriter.clar'
clarity_version = 4
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.lints]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
